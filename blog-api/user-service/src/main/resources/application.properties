spring.application.name=user-service

server.port=8081

spring.config.import=optional:file:blog-api/.env[.properties]

#eureka server config
eureka.client.registerWithEureka=true
eureka.client.fetchRegistry=true
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.hostname=localhost
eureka.instance.preferIpAddress=true

spring.datasource.url=jdbc:mysql://localhost:3306/userdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_LOCAL_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

#spring security
app.jwt-secret-key=${JWT_SECRET_KEY}
app.jwt-expiration-milliseconds= 604800000

